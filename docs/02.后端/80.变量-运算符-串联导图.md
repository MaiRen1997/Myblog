---
title: 变量-运算符-串联导图
date: 2025-12-22 08:35:27
permalink: /pages/56e5ce/
categories:
  - 后端
  - Python学习
tags:
  - 
author: 
  name: Riverside Joy
  link: https://github.com/MaiRen1997
---
# 运算符与变量

## 变量

### 变量

1. 定义规范(6点)
2. 推荐方式(大驼峰，下划线)
3. 等号右边简单类型，赋值值，复杂类型赋值地址
4. **获取变量的内存地址**

### 注释

1. 单行注释： 使用#
2. 多行注释： 使用'''  注释内容  ''' 或者"""  注释内容 """

### 常量

1. 约定俗成，变量名全大写，代表常量

### 获取用户输入

1. input， 结果是字符串

## 运算符

### 常用运算符

1. 下标，切片: [] 、[:]
2. 指数： **
3. 乘、除、模、取整： *、/、% 、 //
4. 加减： +、 -
5. 左移、右移
6. 按位与
7. 按位异或、按位或
8. 小于等于、小于、大于、大于等于
9. 等于、不等于
10. 身份运算符
11. 成员运算符
12. 逻辑运算符
13. (复合)赋值运算符

### 算术运算符(7个)

### 比较运算符

1. 有哪7个
2. == 与is 二者的区别

### 赋值运算符(8个)

1. 有哪八个(7个算数运算符赋值 + 直接赋值)

### 逻辑运算符

1. 有哪三个

### 成员运算符

1. 是否在集合中
2. 是否不在集合中

### 运算符优先级

[详细见文档](https://mairen1997.github.io/Myblog/pages/2d957c/#python-%E8%BF%90%E7%AE%97%E7%AC%A6%E4%BC%98%E5%85%88%E7%BA%A7)

## 格式化输出

### 传统%

1. `%s` - 字符串

2. `%d` - 十进制整数

3. `%f` - 浮点数

4. `%x` - 十六进制整数

5. `%o` - 八进制整数

6. `%e` - 科学计数法

7. `%g` - 自动选择 %f 或 %e

8. `%%` - 输出 % 字符

### str.format()

## f-string()

### 基本使用

1. 字符串拼接
2. 在print中，拼接变量
3. 单、双引号实现单行字符
4. 三引号实现多行字符

### 括号内可用类型

1. 变量
2. 表达式
3. 方法和API
4. 条件表达式、字典列表等复杂类型的API

### 输出格式化

1. : 指定字符   方向  宽度   .保留小数位f
   1. 方向有<   >   ^ （左、右、两边）

### 进制输出

1. 分类
   1. 什么都不填
   2. : b
   3.  `:o`
   4.  :x
   5. :X
2. 输出带前缀，前面加#

### 特殊格式

1. 千分位
2. 百分比
3. 科学计数法
4. 正负号显示

### 时间日期格式化

1. Y-m-d  H:M:S   2024-01-15 14:30:45
2. 其余详细参照笔记

## 流程控制语句

1. if elif else
2. while
   1. 终止循环(6个方式)
3. while ...... else 语句
4. for 循环
   1. 循环字符串
   2. 循环数字
   3. 循环数组
   4. 循环对象
5. enumerate
   1. index ,value 
   2. 下标起始位置自定义
6. range
   1. 设置范围
   2. 设置步长
   3. 设置反向步长

# 路径操作

1. 路径拼接
2. 获取当前文件路径
3. 获取父目录
4. 获取文件名
5. 获取文件名(无扩展)
6. 获取扩展名
7. 获取绝对路径
8. 检查路径是否存在
9. 检查是否为文件(或者目录)
10. 创建目录
11. 遍历目录
12. 路径标准化

# 文件操作

## 文件操作模式

1. 只读
2. 只写
3. 追加
4. 写读
5. 读写

## open与with open

### open的参数

- file: 必需，文件路径（相对或者绝对路径）。

- mode: 可选，文件打开模式

- buffering: 设置缓冲

- encoding: 一般使用utf8

- errors: 报错级别

- newline: 区分换行符

- closefd: 传入的file参数类型

- opener: 设置自定义开启器，开启器的返回值必须是一个打开的文件描述符。

### 打开模式

| 模式 | 描述                                                         |
| :--- | :----------------------------------------------------------- |
| t    | 文本模式 (默认)。                                            |
| x    | 写模式，新建一个文件，如果该文件已存在则会报错。             |
| b    | 二进制模式。                                                 |
| +    | 打开一个文件进行更新(可读可写)。                             |
| U    | 通用换行模式（**Python 3 不支持**）。                        |
| r    | 以只读方式打开文件。文件的指针将会放在文件的开头。这是默认模式。 |
| rb   | 以二进制格式打开一个文件用于只读。文件指针将会放在文件的开头。这是默认模式。一般用于非文本文件如图片等。 |
| r+   | 打开一个文件用于读写。文件指针将会放在文件的开头。           |
| rb+  | 以二进制格式打开一个文件用于读写。文件指针将会放在文件的开头。一般用于非文本文件如图片等。 |
| w    | 打开一个文件只用于写入。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。 |
| wb   | 以二进制格式打开一个文件只用于写入。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。一般用于非文本文件如图片等。 |
| w+   | 打开一个文件用于读写。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。 |
| wb+  | 以二进制格式打开一个文件用于读写。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。一般用于非文本文件如图片等。 |
| a    | 打开一个文件用于追加。如果该文件已存在，文件指针将会放在文件的结尾。也就是说，新的内容将会被写入到已有内容之后。如果该文件不存在，创建新文件进行写入。 |
| ab   | 以二进制格式打开一个文件用于追加。如果该文件已存在，文件指针将会放在文件的结尾。也就是说，新的内容将会被写入到已有内容之后。如果该文件不存在，创建新文件进行写入。 |
| a+   | 打开一个文件用于读写。如果该文件已存在，文件指针将会放在文件的结尾。文件打开时会是追加模式。如果该文件不存在，创建新文件用于读写。 |
| ab+  | 以二进制格式打开一个文件用于追加。如果该文件已存在，文件指针将会放在文件的结尾。如果该文件不存在，创建新文件用于读写。 |

# 数据结构

## 布尔类型

1. true false
2. bool()类型转换

## 数字类型

1. 加减乘除，四则运算
2. 类型转换
   1. float
   2. int

## 字符串

1. 单、双引号，三单引号，三双引号
2. 字符串 + 号拼接
3. 字符串乘数字
4. 索引、切片(顾头不顾腚)
5. 统计字符出现个数
   1. 可统计、可切片(部分)统计
6. 以....字符(字符串) 开头/结尾
   1. 可切片统计
   1. startwith、endwith、startswith、endswith、
7. split(分割符, 次数)、rsplit(分割符, 次数)与splitlines分割
8. format的占位符、索引、具名参数
9. strip、lstrip、rstrip去除
10. replace
   1. 第三个参数count， 设置最大替换次数
11. isalnum、isalpha、isdecimal、isdigit
12. 判断元素是否存在
    1. find/rfind
    2. index/rindex
    3. in/not in
13. 首字母大写、大小写翻转、单词首字母大写
14. 字符填充
    1. center
    2. ljust\rjust
    3. zfill

15. 合并(join)/分割(split)

## 列表list

1. 列表创建
2. 索引 与 切片
   1. 索引访问
   2. 切片访问
   3. 带步长切片
   4. 反向切片

3. 新增
   1. append 最后追加
   2. insert 任意位置追加
   3. extend  迭代着追加
4. 删
   1. pop
   2. remove
   3. clear
   4. del + 切片 实现删除
5. 改
   1. 按索引值改
   2. 按切片改
   3. 按切片+步长改
6. 循环查询
7. 其他API
   1. 配合range填充元素
   2. 比较对应位置元素是否相等
   3. 比较大小
   4. join
   5. len
   6. count
      1. count参数只能有一个，和字符串的入参个数不一致

   7. 第一次出现位置索引(index)
   8. 排序(升序、降序)
   9. 不改变原变量的sorted
   10. reverse反转
   11. 列表相加、相乘
   12. 列表嵌套
   13. 列表拼接
   14. 列表重复声明元素
   15. 是否包含成员
   16. 列表遍历
       1. for循环
       2. for+range


## 元组

1. 创建元组
   1. 一元组的逗号

2. 切片
3. for循环查询
4. index 查找特定元素
5. 统计元素出现次数
6. 获取元组长度
7. 成员运算符
8. 元组拼接
9. 打包和解包
10. 交换两个变量的值
11. 元组VS列表

## 字典

1. 字典定义
   1. 可变数据类型无法做字典key
2. 增(2个)
3. 删(4个)
4. 改(2个)
5. 查(5个)
6. 循环字典
7. 拆包(解构)
8. 字典的嵌套

## 集合set

1. 创建
   1. 包含一个元素的{}
   2. 实用set关键字
   3. 实用set关键字，将列表转为集合
   4. 生成式语法
2. 增
   1. add
   2. update

3. 删(5个)
4. 循环遍历
5. 成员运算
6. 交、并、差、反交集
   1. 交(3个)
   2. 并(2个)
   3. 差(2个)
   4. 反交(2个)

7. 判断是否相等
8. 子集、超集
   1. 子集(2个)
   2. 超集(2个)

9. 对称差(2个)
10. 不可变集合(1个)

## 类型转换

1. 查看类型
2. 简单类型转换
   1. bool
   2. int
   3. float
   4. str
   5. chr
   6. ord
3. 复杂类型转换
   1. str list 两者转换
   2. list 集合(set) 两者转换
   3. str bytes两者转换
4. 所有类型转换成布尔为false的情况

## 深浅拷贝

1. 浅拷贝，用赋值
2. 深拷贝，用copy.deepcopy

# 函数的使用

## 定义、调用及返回值

1. 返回 None、一个值、多个值

## 参数

### 实参

#### 位置参数

#### 关键字参数

#### 混合参数

### 形参

#### 位置参数

#### 默认值参数

#### 动态参数

##### *args

##### *kwords

#### 仅限关键字参数

1. 仅限关键字参数是python3x更新的新特性，
2. 他的位置要放在*args后面，**kwargs前面（如果有**kwargs），也就是默认参数的位置，
3. 它与默认参数的前后顺序无所谓，它只接受关键字传的参数

### 形参位置总结

位置参数，*args，默认参数，仅限关键字参数，**kwargs

### *的其他使用

#### 聚合

#### 打散

#### 处理剩下元素

## 名称空间

### 分类

1. 全局、局部、内置

### 加载顺序

### 取值顺序

## 作用域

1. 全局作用域
2. 局部作用域

## 函数嵌套

## 函数名的用法

1. 赋值给变量
2. 当作容器类元素(将变量名放到字典等容器中，然后遍历调用)
3. 函数名当另一个函数的参数
4. 函数名作返回值

## 迭代器

### 如何判断是否可迭代

### 什么是迭代器

### 可迭代对象与迭代器的对比

## 生成器

## 内置函数

### print

### int

### str

### bool

### set

### list

### tuple

### dict

### abs

### sum

### min

### max

### reversed

### bytes

### zip

### sorted

### filter

### map

### reduce

### enumerate

### open

### range

### len

### type

### dir

## 匿名函数、闭包

### 匿名函数

- 函数名 = lambda 参数:返回值

### 闭包

1. 闭包构成条件三要素
   1. 函数嵌套
   2. 内部函数作为外部函数返回值
   3. 内部函数引用外部函数的变量

## 装饰器

### 基础装饰器

1. 保证闭包三要素
2. 外层函数的形参是func
3. 内部函数调用func，内部函数的形参式 func的形参

### 带返回值装饰器

1. 内部函数只需要将结果return即可

### 被装饰函数带参数

1. 外部函数，形参接收被装饰方法
2. 内部函数，接收被装饰方法的变量

### 标准装饰器

1. 使用`@+函数名语法糖`， @timer 等价于  # home = timer(home)

### 带参数装饰器

1. 相对于不带参数装饰器，外面多一层级函数，用于接收装饰器参数

### 取消装饰器

1. 需要额外在内部方法上，使用@wraps(func)
2. 调用时，使用`原方法.__wrapped__(参数)`即可

# 模块化

## 自定义模块

### import导入

#### 直接导入

#### 取别名

### from ...  import 导入

#### 导入方法

#### 一行导入多个

#### 控制哪些方法可以导出

### py文件的两种方式

#### 当作脚本

1. 一个文件就是整个程序，用来被执行
2. `__name__` 等于`__main__`

#### 当作模块

1. `__name__`等于模块名

#### 模块的搜索路径

1.  **内存中已经加载的模块->内置模块->sys.path路径中包含的模块**

## 内置模块

### 序列化模块

#### json模块

1. dumps、loads
2. dump、load

#### pickle模块

1. dumps、loads
2. dump、load

#### shelve模块

### os模块(特别重要，需要看文档，有哪些api)

### sys模块

### hashlib模块

1. 普通加密
2. 加盐加密
   1. 固定的盐
   2. 动态的盐

### log模块

### random模块

# 类

## 基础使用

### 定义类

1. 类型使用大驼峰

### 访问类的属性

1. `__dict__`访问, dict 可以查看所有属性
2. 点的方式访问
3. 使用getattr方法： `print(getattr(obj, 'mind', '空'))`

### 访问类方法

1. `静态方法、类方法`可以通过类名直接访问

### 动态方法

1. 在类定义时，没有定义，通过 点 操作，动态赋值的属性
2. 通过点赋值、访问、修改
3. 使用`__slots__ = ('name', 'age')`,禁止动态属性，只允许有name 和age属性

### 实例化

#### 实例化发生的三个步骤

1. 内存开辟空间
2. 将内存地址，传给`__init__`方法的第一个self参数
3. 执行init方法，如果内部有属性赋值给self操作，则进行赋值

## 对象内方法分类

### 实例方法

1. 直接在类中定义方法
2. 必须通过实例进行调用
3. 第一个参数是self

### 类方法

1. 使用@classmethod装饰器
2. `类名.方法名`  或者 `实例.方法名`   调用
3. 第一个参数是cls
4. 用来访问、修改类属性

### 静态方法

1. 使用@staticmethod装饰器
2. 通过类名、实例   调用
3. 没有cls 或者self 参数

### 抽象方法

1. 使用@abstractmethod装饰器
2. 定义
   1. 只有方法声明，没有具体实现
   2. 必须在子类中被重写（实现）
   3. 不能直接实例化包含抽象方法的类
3. 作用
   1. 定义接口规范
   2. 强制子类实现特定方法
   3. 实现多态性

### 参数总结

1. 实例方法，第一个参数是self
2. 类方法，第一个参数是cls
3. 静态方法和抽象方法没有默认的参数

## 类空间

1. 对象查找属性的顺序：先从对象空间找 ------> 类空间找 ------> 父类空间找 ------->.....
2. 类名查找属性的顺序：先从本类空间找 -------> 父类空间找--------> ........

## 继承

### 继承分类

| 继承类型     | 写法                             | 关键点                     |
| :----------- | :------------------------------- | :------------------------- |
| **单继承**   | `class Child(Parent):`           | 使用 `super().__init__()`  |
| **多继承**   | `class Child(Parent1, Parent2):` | 注意多重继承顺序(MRO)      |
| **多层继承** | `class GrandChild(Child):`       | 多层 `super()` 调用        |
| **抽象基类** | `class Child(ABC):`              | 必须实现 `@abstractmethod` |

### 单继承

#### 执行顺序

1. 实例化对象时必须执行`__init__`方法,类中没有，从父类找，父类没有，从object类中找
2. 先要执行自己类中的eat方法，自己类没有才能执行父类中的方法。

#### 同时执行类、父类、实例方法

1. 父类.func
2. super().func()

### 多继承

1. 遵守MRO顺序

### 类的约束

1. 在父类定义方法，子类继承
2. 在元类定义抽象方法，各子类继承，然后重写抽象方法

### super的使用及MRO

#### super的执行顺序

1. **super是严格按照类的继承顺序执行**

### MRO的顺序

1. 单层继承的MRO顺序
2. 多层继承的MRO顺序
3. 改变Super的MRO的起点

## 类的成员

### 公有/私有成员

1. 公有静态字段
   1. 哪三个地方可以访问
2. 私有静态字段
   1. 如何定义
   2. 哪地方那个可以访问
   3. 类外如何访问
3. 保护静态字段
   1. 如何定义

### 特性property

### isinstace 与issubclass

### 函数VS方法

## 内置双下方法

### `__len__`

### `__hash__`

### `__str__`

### `__repr__`

### `__call__`

### `__eq__`

### `__del__`

### `__new__`









