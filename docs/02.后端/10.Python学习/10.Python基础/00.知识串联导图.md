# 运算符与变量

## 变量

### 变量

#### 定义规范

1. 数字字母下划线
2. 不能以数字开头
3. 不能使用关键字
4. 不能使用中文
5. 不能过长
6. 推荐使用大驼峰 / 下划线

#### 注意

1. 如果等号右边是简单类型，那么就赋值的值
2. 如果等号右边是复杂类型，那么就赋值的地址

#### 获取变量地址

1. 通过id关键字获取

2. ```go
   name = '测试'
   print(id(name))  # 1585831283968
   ```

### 注释

1. 单行注释： 使用#
2. 多行注释： 使用'''  注释内容  ''' 或者"""  注释内容 """

### 常量

1. 没有专门的语法定义常量，默认约定使用全大写代表常量

2. ```go
   PI = 3.1415926
   ```

## 运算符

### 常用运算符

1. 下标，切片: [] 、[:]
2. 指数： **
3. 乘、除、模、取整： *、/、% 、 //
4. 加减： +、 -
5. 左移、右移
6. 按位与
7. 按位异或、按位或
8. 小于等于、小于、大于、大于等于
9. 等于、不等于
10. 身份运算符
11. 成员运算符
12. 逻辑运算符
13. (复合)赋值运算符

### 算术运算符

1. 加
2. 减
3. 乘
4. 除
5. 取模
6. 幂
7. 取整除(返回商的整数部分)

### 比较运算符

1. 是否等于
2. 是否不等于
3. 不等于
4. 大于
5. 小于
6. 大于等于
7. 小于等于
8. 注意
   1. == 是判断两者值是否相等，is 是判断两者内存地址是否相等

### 赋值运算符

1. 赋值
2. 加后赋值
3. 减后赋值
4. 乘后赋值
5. 除后赋值
6. 取模赋值
7. 幂运算后赋值
8. 取整除赋值

### 逻辑运算符

1. 布尔与
2. 布尔或
3. 布尔非

### 成员运算符

1. 是否在集合中
2. 是否不在集合中

### 运算符优先级

[详细见文档](https://mairen1997.github.io/Myblog/pages/2d957c/#python-%E8%BF%90%E7%AE%97%E7%AC%A6%E4%BC%98%E5%85%88%E7%BA%A7)

## 程序交互

1. 使用input关键字，接收用户输入

2. input创建的变量都是字符串

3. ```go
   # 可以让用户输入多个信息，
   name = input("What is your name?")
   age = input("How old are you?")
   hometown = input("Where is your hometown?")
   
   print("Hello ",name , "your are ", age , "years old, you came from",hometown)
   ```

## 格式化输出

### 传统%

1. `%s` - 字符串

2. `%d` - 十进制整数

3. `%f` - 浮点数

4. `%x` - 十六进制整数

5. `%o` - 八进制整数

6. `%e` - 科学计数法

7. `%g` - 自动选择 %f 或 %e

8. `%%` - 输出 % 字符

### str.format()

### f-string()

# 流程控制语句

## if语句

1. 使用关键字`if`、`elif` 、`else`

2. 不需要小括号

3. 不需要大括号，使用缩进代替

## while语句

### 基本使用

### 终止循环

1. break
2. continue
3. quit
4. exit
5. pass

## while...else语句

## for循环

1. 循环字符串
2. 循环数字
3. 循环数组
4. 循环对象

## 枚举enumerate

## range

1. 起始数字、终止数字、步长
2. 反向步长

# 文件操作

## 文件操作模式

1. 只读
2. 只写
3. 追加
4. 写读
5. 读写

## open与with open

### open的参数

- file: 必需，文件路径（相对或者绝对路径）。

- mode: 可选，文件打开模式

- buffering: 设置缓冲

- encoding: 一般使用utf8

- errors: 报错级别

- newline: 区分换行符

- closefd: 传入的file参数类型

- opener: 设置自定义开启器，开启器的返回值必须是一个打开的文件描述符。

### 打开模式

| 模式 | 描述                                                         |
| :--- | :----------------------------------------------------------- |
| t    | 文本模式 (默认)。                                            |
| x    | 写模式，新建一个文件，如果该文件已存在则会报错。             |
| b    | 二进制模式。                                                 |
| +    | 打开一个文件进行更新(可读可写)。                             |
| U    | 通用换行模式（**Python 3 不支持**）。                        |
| r    | 以只读方式打开文件。文件的指针将会放在文件的开头。这是默认模式。 |
| rb   | 以二进制格式打开一个文件用于只读。文件指针将会放在文件的开头。这是默认模式。一般用于非文本文件如图片等。 |
| r+   | 打开一个文件用于读写。文件指针将会放在文件的开头。           |
| rb+  | 以二进制格式打开一个文件用于读写。文件指针将会放在文件的开头。一般用于非文本文件如图片等。 |
| w    | 打开一个文件只用于写入。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。 |
| wb   | 以二进制格式打开一个文件只用于写入。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。一般用于非文本文件如图片等。 |
| w+   | 打开一个文件用于读写。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。 |
| wb+  | 以二进制格式打开一个文件用于读写。如果该文件已存在则打开文件，并从开头开始编辑，即原有内容会被删除。如果该文件不存在，创建新文件。一般用于非文本文件如图片等。 |
| a    | 打开一个文件用于追加。如果该文件已存在，文件指针将会放在文件的结尾。也就是说，新的内容将会被写入到已有内容之后。如果该文件不存在，创建新文件进行写入。 |
| ab   | 以二进制格式打开一个文件用于追加。如果该文件已存在，文件指针将会放在文件的结尾。也就是说，新的内容将会被写入到已有内容之后。如果该文件不存在，创建新文件进行写入。 |
| a+   | 打开一个文件用于读写。如果该文件已存在，文件指针将会放在文件的结尾。文件打开时会是追加模式。如果该文件不存在，创建新文件用于读写。 |
| ab+  | 以二进制格式打开一个文件用于追加。如果该文件已存在，文件指针将会放在文件的结尾。如果该文件不存在，创建新文件用于读写。 |

















